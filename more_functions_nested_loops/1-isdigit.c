#include "main.h"

/**
 * _isupper - checks if c is uppercase char.
 * @c: is a character
 * Return: 1 if c is uppercase,
 *         0 if it is not uppercase.
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}
